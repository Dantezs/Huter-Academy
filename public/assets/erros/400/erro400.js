// Verifica o código de status da resposta HTTP
if (status === 400) {
    // Redireciona o usuário para a página de erro 400
    window.location.href = "/public/assets/pages/error/erro400.html";
  }
  